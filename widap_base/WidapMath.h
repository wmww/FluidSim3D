#pragma once

#include "WidapStd.h"

int findPrimeFactors(int num, int **answerArray); //finds the prime factors and returns the number of them, it allocates the array but you must delete it

int solveCubic(double a, double b, double c, double d, double answer[3]);

